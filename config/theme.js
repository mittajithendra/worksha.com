const colors = {
  primary: "#4CB6B6", // Color for buttons or links
  primaryLight: "#A1F2F2",
  bg: "#282828",
  grey: {
    dark: "#4CB6B6",
    default: "#B2BECD",
    light: "#EEEEEE",
    ultraLight: "rgba(0, 0, 0, 0.25)",
  },
  white: "white",
}

const transitions = {
  normal: "0.5s",
}

const fontSize = {
  small: "0.9rem",
}

const fontFamily = {
  serif: `'Montserrat', sans-serif`,
  sansSerif: `'Montserrat', sans-serif`,
  monoSpace: `'Source Code Pro', monospace`,
}

const breakpoints = {
  tablet: "1200px",
  phone: "600px",
}

const theme = {
  colors,
  transitions,
  fontSize,
  breakpoints,
  fontFamily,
  maxWidth: "1000px",
  baseFontSize: "18px",
}

export default theme
